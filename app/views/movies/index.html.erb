<div class="container text-center">
  <div class="border-top border-bottom mt-3 mb-3">
    <h1 class="mt-3">SWIPE</h1>
    <h1>MOVIE YOU LIKE</h1>
  </div>
  <div>
    <% @movies.each do |movie| %>
      <div class="blog">
        <div class="title-box">
          <h3><%= movie.title %></h3>
          <%= render 'swipes/form', swipe: @swipe, party: @party, movie: movie %>
      </div>

    </div>
        <% end %>
  </div>
</div>

